# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? |
|---|------|-------|----------|--------|-----------|------------|
| D001 | M001-ykiyqu | library | CSS framework | Tailwind v4 via `@tailwindcss/vite` | Requerido por el usuario. V4 usa Vite plugin directo, sin `tailwind.config.js`. No usar `@astrojs/tailwind` (deprecated para v4). | No |
| D002 | M001-ykiyqu | arch | Componentes UI | Astro puro (`.astro`), sin React/Vue | Requerido por el usuario. Scripts de interactividad via `<script>` en Astro. | No |
| D003 | M001-ykiyqu | convention | Tokens de diseño | `@theme` en `src/styles/global.css` mapeados desde variables del `.pen` | Tailwind v4 usa `@theme` como mecanismo nativo de tokens. Las variables del `.pen` ya están definidas con axis `mode: [light, dark]`. | Yes — si Tailwind v5 cambia la API |
| D004 | M001-ykiyqu | arch | Layout boxed | `max-w-[1440px] mx-auto` en wrapper de cada sección | El usuario especificó max-width 1440px boxed. Las secciones del `.pen` tienen `width: 1440`. | No |
| D005 | M001-ykiyqu | convention | Estructura de componentes | Secciones como `src/components/sections/NombreSeccion.astro` | Cada sección del diseño es un componente independiente. Facilita reuso entre M1 (light) y M2 (dark). | Yes |
| D006 | M001-ykiyqu | convention | Foto hero | `public/profile.webp` | El usuario lo especificó. El placeholder del `.pen` es 320×380px con cornerRadius 8. | No |
| D007 | M001-ykiyqu | library | Fuente | Inter via Google Fonts CDN en `Layout.astro` | El `.pen` usa Inter en todo el design system. CDN es el camino más rápido; puede self-hosted en QA (M006). | Yes — self-host en M006 si afecta performance |
| D008 | M005-9stm2g | library | Animaciones | Motion (vanilla JS, `motion` package) | Compatible con Astro puro. El usuario mencionó "motion" explícitamente. No Framer Motion (requiere React). | Yes — si el usuario prefiere GSAP |
| D009 | M006-5l0p96 | convention | QA methodology | Export PNG de cada sección via MCP Pencil `export_nodes` + comparación visual en browser | Permite validación objetiva pixel por pixel. El MCP de Pencil está disponible y configurado. | No |
| D010 | M001-ykiyqu/S01 | convention | Design tokens mapping strategy (supersedes D003) | `:root` CSS custom properties instead of `@theme` for design tokens. Components use `bg-[var(--bg-primary)]` syntax. | The `.pen` token names are semantic (`--bg-primary`, `--text-heading`, `--border-default`) and don't follow Tailwind v4's `@theme` namespace conventions (`--color-*`, `--font-*`, `--spacing-*`). Using `:root` preserves 1:1 naming with the design file for easy cross-referencing during QA. The Pencil code guidelines also recommend this pattern. Naturally extends to dark mode (M002) by scoping under `.dark` selector. `@theme` would require renaming all tokens to fit Tailwind namespaces, losing the design-to-code traceability. | Yes — if future slices need Tailwind utility generation for these tokens |
| D011 | M001-ykiyqu/S02 | arch | Header landmark placement relative to main content | Header rendered outside `<main>`, Hero and subsequent sections inside `<main>` | The `<header>` is a navigation landmark, not page content. Placing it outside `<main>` follows HTML5 semantic landmarks spec and helps screen reader users distinguish navigation from content. | No |
| D012 | M001-ykiyqu/S04 | convention | Missing color tokens for Skills featured card | Add 3 new `:root` tokens: `--bg-java-card: #0F0A2A`, `--text-accent-light: #E0D7FE`, `--text-accent-desc: #C7D2FE`. Other colors needed by Skills map to existing tokens (`--color-accent-soft`, `--color-accent-lighter`, `--bg-indigo-pill`, `--bg-amber`, `--bg-amber-pill`, `--text-amber-dark`, etc.). | The `.pen` Java featured card uses `#0F0A2A` (deep indigo-black) which is distinct from `--bg-stats` (#374151). The light purple text colors `#E0D7FE` and `#C7D2FE` appear only on the dark card and have no existing token match. Adding named tokens maintains the established pattern (D010) of semantic `:root` custom properties with 1:1 `.pen` traceability. Inlining hex values would break the convention and make dark mode (M002) harder. | Yes — M002 may rename these when adding dark mode variants |
| D013 | M001-ykiyqu/S05 | convention | Amber category/metric text token value | Correct `--text-amber-accent` from `#d97706` to `#b45309` to match the `.pen` design | The `.pen` uses `#B45309` for amber category text and amber metric values. The current token `--text-amber-accent: #d97706` was a mapping error. The token is defined in global.css but not yet consumed by any component, so the correction is safe. `#B45309` is the exact value from the design file for this role. | No |
| D014 | M002-0eaci5 planning | convention | How to handle --color-primary dual usage (text vs background) in dark mode | Override `--color-primary` to `#818CF8` in `.dark {}` for text, introduce new `--color-primary-bg: #4338CA` (constant both modes) for backgrounds. Migrate the 13 bg usages instead of the 83 text usages. | The `.pen` dark frame lightens `--color-primary` to `#818CF8` for text (labels, links, dates — 83 occurrences) but keeps `#4338CA` for button/logo backgrounds (13 occurrences). Migrating the smaller set (13 bg usages → `--color-primary-bg`) is dramatically less work and less risky than migrating 83 text usages to a new `--color-primary-light` token. Research proposed the inverse approach but codebase analysis shows this direction is superior. | No |
| D015 | M002-0eaci5 planning | arch | Dark mode implementation strategy for Tailwind v4 + Astro | CSS custom properties under `.dark {}` selector + `@custom-variant dark (&:where(.dark, .dark *));` for enabling `dark:` Tailwind utilities. Toggle JS adds/removes `class="dark"` on `<html>`. FOUC prevention via `<script is:inline>` in `<head>` reading localStorage. | Tailwind v4 class-based dark mode requires `@custom-variant dark`. The project uses `:root` CSS custom properties (D010), not `@theme`, so dark overrides go in `.dark {}` selector — CSS specificity handles the cascade automatically. `is:inline` script in `<head>` prevents FOUC by applying class before first paint. Vanilla JS toggle (no framework per D002/R003). | Yes — if Tailwind v4 @custom-variant proves incompatible, fall back to pure CSS `.dark` scoping without the Tailwind directive |
| D016 | M002-0eaci5 planning | convention | Header theme toggle sun circle color correction (M001 bug) | Fix sun circle from `bg-[var(--color-primary)]` (#4338CA) to `bg-[var(--color-accent)]` (#7c3aed) as part of M002 toggle conversion | The `.pen` shows the active toggle icon on a purple (#7c3aed / --color-accent) background, not indigo (#4338CA / --color-primary). This was a mapping error in M001 S02. Fixing it during M002's toggle conversion is the natural time — the toggle markup is being rewritten anyway. | No |
| D017 | M003-8vt8w6 planning | convention | Mobile theme toggle appearance | On mobile, show a bare moon/sun icon instead of the pill toggle. The pill toggle is shown only on `md:` and above. | The research identified that the `.pen` mobile frame shows a bare moon icon for theme toggle, not the pill with both sun+moon. This matches standard mobile design patterns where space is constrained. The pill toggle has `hidden md:flex` and the bare icon has `flex md:hidden`. | Yes — if M006 QA reveals the .pen actually shows the pill on mobile |
| D018 | M003-8vt8w6 planning | convention | Mobile theme toggle appearance | On mobile, show a bare moon/sun icon (not the pill toggle). Pill toggle shown only on md: and above. Responsive show/hide with hidden/flex + md: prefix. | The .pen mobile frame NdHGJ shows a bare moon icon for theme toggle, not the pill with both sun+moon. This matches mobile design convention for space-constrained headers. | Yes — if M006 QA reveals the .pen actually shows the pill on mobile |
| D019 | M003-8vt8w6 planning | convention | Theme toggle visual variant for mobile viewport | Bare moon/sun icon on mobile via hidden md:flex / flex md:hidden responsive toggle. Pill toggle desktop-only. | .pen frame NdHGJ shows bare moon icon on mobile header, not the full pill toggle. Space-constrained mobile header favors single icon. | Yes |
| D020 | M004-ahf7ia planning | convention | Overlay chevrons and social icons scope in M004 | Implement chevron arrows and social icons row as cross-cutting additions in M004, affecting both light and dark overlay modes, rather than deferring to a separate milestone | The .pen frames 71HM1 (light) and hhxcX (dark) both show these elements — they were gaps in M003's overlay implementation. Since they use token-based colors that already have dark overrides, adding them in M004 automatically completes both modes. Splitting them into a separate "M003 fix" milestone adds ceremony for no value. The overlay separator color (--border-default vs --bg-card as shown in .pen) is left as-is — the difference is subtle (#2D2D3A vs #1E1E2A in dark) and can be refined in M006 QA. | No |
| D021 | M005-9stm2g planning | architecture | Animation script placement in Astro components | Each section component gets its own `<script>` tag importing from `motion` and `src/lib/motion.ts`. Use default Astro module scripts (deferred), NOT `is:inline`. Hero uses direct `animate()` on load; all other sections use `inView()` for scroll triggers. | Astro module scripts are deferred (run after DOM ready), which is ideal for animation init — content renders first, then animates in. Per KNOWLEDGE.md, `is:inline` is reserved for FOUC prevention only. Astro deduplicates and bundles module scripts automatically. Each section's animation logic is self-contained in its component, matching the established pattern where only Header.astro has a `<script>` currently. | No |
| D022 | M005-9stm2g planning | architecture | CSS-only vs motion JS hover split | Simple hovers (color/opacity on nav links, footer links, CTA buttons) remain CSS-only via existing Tailwind `hover:` utilities. Complex hovers (translateY + boxShadow + border-color on cards) use `motion`'s `hover()` API with runtime CSS variable reads for dark-mode awareness. | The .pen prescribes two hover categories: simple (already handled by Tailwind `hover:` + `transition`) and complex (multi-property coordination with dark-mode-specific values). Using `hover()` for the complex set avoids fighting Tailwind's JIT scanner with dynamic class names (per KNOWLEDGE.md "Icon Visual State" entry) and allows reading computed CSS variable values at runtime for theme-aware transitions. | Yes — if all hover states can be expressed purely in CSS custom properties + Tailwind transitions |
| D023 | M005-9stm2g planning | scope | CountUp animations scope exclusion | CountUp animations are excluded from M005 scope. The .pen mentions them but the milestone context's In Scope section does not list them. They can be added in M006 QA if the visual comparison reveals they're expected. | CountUp uses a distinct pattern (`animate(0, N, { onUpdate })`) separate from the core entrance/hover animations. The milestone context explicitly lists scroll-triggered entrances, staggered reveals, hover micro-interactions, and prefers-reduced-motion as in-scope. CountUp is mentioned only in research as an open risk. Adding it would expand scope without clear mandate. | Yes — if user requests it or M006 QA flags it as missing |
| D024 | M005-9stm2g planning — user-initiated library swap before S01 execution | library | Animation library for M005 milestone | Replace `motion` (Framer Motion vanilla JS) with `animejs` (anime.js v4). Supersedes D008. | User explicitly requested the change. anime.js v4 provides equivalent capabilities: `animate()` for animations, `onScroll()` for scroll-triggered viewport observation, `stagger()` for staggered delays. It's a pure vanilla JS library with no React dependency concern (unlike `motion` which shares a monorepo with Framer Motion React). Modular imports allow tree-shaking. The API maps cleanly to the planned animation patterns: `autoplay: onScroll({ enter, leave })` replaces `inView()`, and `animate(targets, props, { delay: stagger(N) })` is equivalent. | No |
| D025 | M005-9stm2g planning phase — user-initiated deferral | scope | Whether to proceed with the animations milestone (M005) | Remove M005-9stm2g entirely. Defer R007 (animations) to a future milestone with library TBD. Supersedes D008, D021, D022, D023, D024. | User decided to defer animations work to a later time. The animation library choice (motion vs animejs vs other) is left open for that future milestone. | Yes |
| D026 | M006-5l0p96 planning | convention | How to handle --text-amber-accent light value conflict between CSS (#b45309 per D013) and .pen (#d97706) | Resolve during S01 Token Audit by visually comparing both values against the .pen rendered output. The .pen is the absolute source of truth (per PROJECT.md). If the .pen renders with #d97706, CSS should be updated to match. D013's correction to #b45309 was based on a .pen reading that may have been wrong — the current get_variables call confirms the .pen variable is #d97706. | The .pen file is the design source of truth. D013 explicitly states it corrected the value to match the .pen, but the actual .pen variable is #d97706 (confirmed via MCP get_variables). This suggests D013 may have been based on a misread of the .pen. S01 will verify visually and correct. | Yes — S01 execution will make the final determination |
| D027 |  | convention | Correct --text-amber-accent light value (supersedes D013) | Revert to #d97706 matching the .pen source of truth | MCP get_variables confirms .pen defines text-amber-accent light as #d97706. D013 incorrectly changed it to #b45309 based on a misread of the .pen. The .pen is the absolute design source of truth per PROJECT.md. T01 corrected the CSS value; this decision formally supersedes D013. | Yes |
| D028 | M009-lxoyrb | convention | Source of truth for M009 visual QA | Screenshot pairs (`_design/screenshots/design/` vs `_design/screenshots/dev/`) are the final source of truth for visual and content discrepancies. The .pen MCP is a secondary reference only if screenshot text is illegible. | User explicitly stated that the visual comparison between screenshot pairs is the authoritative source, not the MCP. This prevents inconsistencies where MCP data and rendered output diverge (as seen in M007 with the Skills section text). | No |
| D029 | M010-jgngfg | convention | Screenshot folder structure for cross-mode visual QA | Structured folders: `_design/screenshots/{design\|dev}/{light\|dark}/{mobile\|desktop\|wide}/001-009.png` | Enables 1:1 comparison between design references and dev captures. The `wide/` subfolder (1600px) exists only under `dev/` since there's no .pen design at that width — used purely for max-width constraint validation. Naming consistency (001–009) across all folders makes comparison straightforward. | No |
